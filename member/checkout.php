<?php
require_once __DIR__.'/../bootstrap/app.php';use GymPro\Services\SubscriptionService;require_role('MEMBER');$u=current_user();$sid=(int)($_GET['subscription_id']??$_POST['subscription_id']??0);$s=db_one("SELECT * FROM SUBSCRIPTION WHERE SubscriptionID=? AND MemberID=? AND Status='PENDING_PAYMENT'",'ii',[$sid,$u['MemberID']]);if(!$s){set_flash('danger','Pending subscription not found.');redirect('member/membership.php');}$errors=[];$key=$_POST['idempotency_key']??bin2hex(random_bytes(24));
if($_SERVER['REQUEST_METHOD']==='POST'){verify_csrf();try{$result=SubscriptionService::pay($sid,(int)$u['MemberID'],(int)$u['UserID'],$_POST);set_flash('success','Demo payment accepted. Reference: '.$result['TransactionReference']);redirect('member/index.php');}catch(Throwable $e){$errors[]=$e->getMessage();}}
$pageTitle='Simulated checkout';$pageSubtitle='Educational demonstration — no real payment is processed';include ROOT_PATH.'/includes/header.php';?>
<div class="demo-banner"><strong>Demo payment only</strong><span>Enter any syntactically valid card details. GymPro never stores the full card number, expiry, or CVV.</span></div>
<?php if($errors):?><div class="alert alert-danger" role="alert"><?=e(implode(' ',$errors))?></div><?php endif;?>
<div class="form-card"><div class="checkout-summary"><span><?=e($s['PlanNameSnapshot'])?></span><strong>Rs.<?=number_format((float)$s['PriceSnapshot'],2)?></strong></div><form method="post" autocomplete="off"><?=csrf_field()?><input type="hidden" name="subscription_id" value="<?=$sid?>"><input type="hidden" name="idempotency_key" value="<?=e($key)?>"><div class="form-grid"><div class="form-group form-full"><label for="card_name">Name on card</label><input id="card_name" name="card_name" required autocomplete="off"></div><div class="form-group form-full"><label for="card_number">Card number</label><input id="card_number" name="card_number" inputmode="numeric" minlength="12" maxlength="23" placeholder="4242 4242 4242 4242" required autocomplete="off"></div><div class="form-group"><label for="expiry">Expiry (MM/YY)</label><input id="expiry" name="expiry" pattern="(0[1-9]|1[0-2])/\d{2}" placeholder="12/30" required autocomplete="off"></div><div class="form-group"><label for="cvv">CVV</label><input id="cvv" type="password" name="cvv" inputmode="numeric" minlength="3" maxlength="4" required autocomplete="off"></div></div><div class="form-actions"><button class="btn btn-success" type="submit">Complete demo payment</button><a class="btn btn-ghost" href="membership.php">Cancel</a></div></form></div>
<?php include ROOT_PATH.'/includes/footer.php';?>
