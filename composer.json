{
  "name": "university/gympro",
  "description": "Role-based gym membership management system",
  "type": "project",
  "require": {
    "php": ">=8.1",
    "ext-mysqli": "*",
    "ext-fileinfo": "*",
    "phpmailer/phpmailer": "^6.9",
    "vlucas/phpdotenv": "^5.6"
  },
  "require-dev": {
    "phpunit/phpunit": "^10.5"
  },
  "autoload": {
    "psr-4": {
      "GymPro\\": "app/"
    }
  },
  "scripts": {
    "test": "phpunit",
    "lint": "@php -r \"foreach (new RecursiveIteratorIterator(new RecursiveDirectoryIterator('.', FilesystemIterator::SKIP_DOTS)) as $f) if ($f->getExtension()==='php' && !str_contains($f->getPathname(), DIRECTORY_SEPARATOR.'vendor'.DIRECTORY_SEPARATOR)) passthru(PHP_BINARY.' -l '.escapeshellarg($f->getPathname()), $e);\""
  }
}
